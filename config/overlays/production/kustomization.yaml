apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../default

# Supply environment-specific image tags, public hosts, LiveKit keys, S3
# credentials, storage class, and TLS/L4 exposure in a private overlay. The
# checked-in Secret values are development placeholders and are not suitable
# for production.
