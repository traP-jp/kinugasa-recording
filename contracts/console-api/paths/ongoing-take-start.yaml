parameters:
  - $ref: ../openapi.yaml#/components/parameters/SessionName

post:
  operationId: startTake
  tags: [Takes]
  summary: OngoingTakeを作成して録画を開始する
  description: connectedなCameraConnectionを持つcameraを1つ以上指定する必要がある。指定したすべてのcameraがこの条件を満たさなければならない。
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../schemas/take.yaml#/StartTakeRequest
  responses:
    '201':
      description: 作成したOngoingTake
      content:
        application/json:
          schema:
            $ref: ../schemas/take.yaml#/OngoingTake
    '400':
      $ref: ../openapi.yaml#/components/responses/BadRequest
    '404':
      $ref: ../openapi.yaml#/components/responses/NotFound
    '409':
      description: OngoingTakeがすでに存在する、名前が使用済み、または指定したcameraのCameraConnectionがconnectedではない
      content:
        application/json:
          schema:
            $ref: ../schemas/error.yaml#/Error
    '500':
      $ref: ../openapi.yaml#/components/responses/InternalServerError
