# 性能要求

## 録画品質

本項は適合性の必須条件ではなく、録画品質に関する努力目標とする。

H.264形式、30 fpsの複数映像を10分間録画した場合、規定のネットワークジッタ条件下におけるcamera間の時間軸ドリフトは、任意のcameraの組について2 frame以下を目標とする。

時間軸ドリフトは、録画開始付近と録画終了付近におけるcamera間の時間オフセットの差の絶対値として定義する。30 fpsにおける2 frameは約66.7 msである。
